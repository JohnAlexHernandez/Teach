<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asesoriasacademicasweb.asesoriasacademicas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AsesoriasAcademicas">
        <activity android:name=".EditarPerfilActivity"></activity>
        <activity android:name=".EditarClaseActivity" />
        <activity android:name=".PopupDetalleClaseActivity" />
        <activity android:name=".GestionarClaseActivity" />
        <activity android:name=".GestionarClaseActivity" />
        <activity android:name=".SolicitarClaseActivity" />
        <activity android:name=".RegistrarseActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>